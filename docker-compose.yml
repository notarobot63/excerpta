services:
  linky:
    build: .
    container_name: linky
    restart: unless-stopped
    ports:
      - "8070:8000"
    env_file: .env
    volumes:
      - linky_data:/app/data

volumes:
  linky_data:
