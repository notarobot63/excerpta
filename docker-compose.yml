services:
  excerpta:
    build:
      context: .
      args:
        GIT_COMMIT: ${GIT_COMMIT:-unknown}
    container_name: excerpta
    restart: unless-stopped
    ports:
      - "8070:8000"
    env_file: .env
    volumes:
      - excerpta_data:/app/data

volumes:
  excerpta_data: